/**
 * Tipografías disponibles en el editor y en el PDF (definidas en el modelo compartido).
 * Se cargan desde Google Fonts tanto en el editor como en el renderer,
 * por eso el PDF usa exactamente las mismas fuentes que se ven en pantalla.
 */
export { FONTS, DEFAULT_FONT, fontWeights, googleFontsUrl, fontStack } from '../shared/renderModel.js';
