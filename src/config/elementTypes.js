/**
 * Tipos de elemento y tokens de color (definidos en el modelo compartido).
 * Para un tipo nuevo: agregarlo en src/shared/renderModel.js (y su copia del
 * frontend) y en el registro de elementos del editor.
 */
export { ELEMENT_TYPES, TEXT_TYPES, COLOR_TOKENS } from '../shared/renderModel.js';
